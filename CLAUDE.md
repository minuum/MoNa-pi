# MoNa-pi 프로젝트 작업 규칙

## 핵심 원칙
**작성된 계획을 사용자가 직접 검토하고 승인하기 전까지 코드를 절대 쓰지 마라.**

## 작업 순서 (김플립 5단계)

### 1. 코드리서치
- 관련 파일을 깊이 읽고 동작 방식을 완전히 이해한다
- 기존 레이어/ORM 관례/중복 API를 먼저 파악한다
- 끝나면 `research.md`에 상세 보고서 작성

### 2. 계획
- `plan.md` 작성: 접근 방식, 코드 스니펫, 수정 파일 경로, 트레이드오프
- **계획만 작성. 구현 금지.**

### 3. 주석달기 (사용자 검토 후)
- 사용자가 plan.md에 메모를 추가하면 반영하여 계획 업데이트
- 사용자 승인이 날 때까지 반복
- **"아직 구현하지 마" — 승인 전 코드 금지**

### 4. 기계적 구현
- 승인 후 전부 구현
- 작업/단계 완료 시 plan.md에서 완료 표시
- `any`/`unknown` 타입 사용 금지
- 지속적으로 typecheck 실행

### 5. Feedback
- 잘못된 방향 → `git reset`/`git revert` 후 재시작 (조금씩 고치는 것보다 항상 낫다)

---

## 커뮤니케이션 스타일
- 한국어로 대화, 코드/파일명은 영어 유지
- 짧고 직접적으로. 불필요한 설명 금지
- 논문(π0)과 구현 격차 발견 시 → 즉시 proactive하게 보고
- 아키텍처 질문 시 솔직하게 격차 인정

## 프로젝트 컨텍스트
- 모델: `action_expert` 직접 참조 (`flow_head` alias 없음)
- `use_paligemma=True` 기본, `load_pretrained_paligemma=False` 기본 (로컬 캐시)
- Jetson AGX 환경, FP16, 액션 공간 3-DOF
