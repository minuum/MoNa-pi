# MoNa-pi 핸드오프 문서
> 작성 시각: 2026-06-01  
> 작성 브랜치: monapi-train  
> 전달 브랜치: monapi-driving  
> 목적: 다른 서버에서 이어서 작업할 수 있도록 현재 상태·지시사항·투두 전달

---

## 1. 현재 학습 상태 (GX10 서버 기준)

### 완료된 학습 실험

| 실험 | 데이터 | Backbone | NOISE | Best val_loss | CL SR | 체크포인트 |
|---|---|---|---|---|---|---|
| v4 (원본 50ep) | v5 116ep | base PaliGemma | 20% | 0.0404 | 100% (과적합) | `epoch_048_loss_0.0404` |
| merged-20 (파인튜닝) | 244ep | base PaliGemma | 10% | 0.0414 | 100% (과적합) | `epoch_012_loss_0.0414` |
| merged-30 (재학습) | 244ep | base PaliGemma | 20% | 0.1440 | 71.7% | `epoch_021_loss_0.1440` |
| **v5 (현재 best)** | 244ep | base PaliGemma | **5%** | **0.1499** | **73.6%** | **`checkpoints/best`** |
| lerobot (진행중) | 244ep | **lerobot/pi0_old** | 5% | Ep1: 0.3994 | TBD | 학습중 |

> **CL SR 측정 기준**: FPE < 0.5m AND TLD ∈ [0.7, 1.5], 53 val episodes  
> **중요**: val 100%는 과적합. v5 모델의 73.6%가 실제 일반화 성능에 가장 근접

### 진행 중인 학습 (GX10)
```
PID: 3751169
LOG: logs/train_lerobot_20260601_1626.log
CONFIG: configs/train.yaml (pretrain.ckpt_path: checkpoints/lerobot_pi0_backbone)
Backbone: lerobot/pi0_old에서 추출한 로봇 데이터 적응 PaliGemma (602/695 레이어)
예상 완료: ~90분
```

---

## 2. 핵심 수치 (발표용)

```
데이터:    244 에피소드 (v5 116 + add_free 94 신규)
CL SR:     73.6% (53 val episodes, offline CL simulation)
CL FPE:    0.387 m
CL TLD:    0.988
추론 레이턴시: ~240ms warm (4Hz @ GX10)
언어 ablation: text 제거 시 FPE +61% (0.673→1.085)
```

---

## 3. 즉시 해야 할 것 (Serbot2 측)

### [최우선] 체크포인트 전송
```bash
# GX10 → Serbot2 (약 5.9GB)
scp -r checkpoints/best/ user@serbot2:/path/to/MoNa-pi/checkpoints/best/

# 전송 전 Serbot2에서 먼저 clone
git clone -b monapi-driving https://github.com/minuum/MoNa-pi.git
cd MoNa-pi
pip install bitsandbytes  # INT8 필수
```

### [6/2] 추론 서버 기동 테스트
```bash
# 스왑 확인 (없으면 설정)
free -h  # swap 8GB 이상 있어야 함
sudo fallocate -l 8G /swapfile && sudo chmod 600 /swapfile
sudo mkswap /swapfile && sudo swapon /swapfile

# 서버 시작 (INT8, ~3GB)
./start.sh server
# 또는
python inference/server.py --config configs/serbot2.yaml --ckpt checkpoints/best --port 8080

# 헬스 체크
curl http://localhost:8080/health
# → {"status":"ok","engine_ready":true,...}
```

### [6/2] 실기동 테스트 순서
```bash
# 전체 스택
./start.sh hybrid

# 키보드 제어 순서:
# 1. M 눌러서 Manual 모드 → WASD로 로봇 동작 확인
# 2. H 눌러서 Hybrid 모드 → VLA 동작 모니터링
# 3. V 눌러서 VLA 모드 → 자율 주행 테스트
# X = 언제든 비상정지
```

### ⚠️ Instruction 반드시 확인
```
[INTENT] 태그 없는 instruction은 모델 성능 급락 (SR ~0%)
configs/serbot2.yaml의 instruction이 자동으로 "[FORWARD] 목표물을 향해 이동해"로 설정됨
시나리오별 변경 필요시:
  "[FORWARD] 직진해"
  "[STRAFE_LEFT] 왼쪽으로 이동해"
  "[TURN_LEFT] 좌회전해"
```

---

## 4. 실기동 결과에 따른 분기

### 성공 시 (SR 70%+)
```
→ 6/4 데모 시나리오 3개 확정
→ 반복 검증 후 발표 준비
```

### 실패 시 (특정 경로 0%)
```bash
# 실패 시나리오 데이터 수집 (ros2_recorder)
python collect/ros2_recorder.py --output-dir data/new_ep/ --n-episodes 30

# 수집된 데이터 merged에 심볼릭 링크
ln -sf $(pwd)/data/new_ep/*.h5 mobile_vla_dataset_merged/

# GX10으로 데이터 전송 후 파인튜닝 요청 (또는 온보드 파인튜닝)
# 파인튜닝은 약 15-30분 소요
```

---

## 5. 발표 관련 주의사항

- **실로봇 미검증 상태로 슬라이드 작성 중**
- 성과를 "구현 및 시뮬레이션 검증"으로 표현 (과장 금지)
- π0 관련: "π0에서 영감받은 구조" — 직접 가중치 사용 아님
- AdaLN-Zero: DiT(2023)에서 차용, π0 논문의 내용 아님
- CL SR 73.6%는 offline simulation 기준임을 명시

---

## 6. 브랜치 구조

```
monapi-train  ← 학습 코드, 실험, 체크포인트 (GX10)
monapi-driving ← 이 브랜치, 배포 전용 (Serbot2)
  inference/    - 추론 서버 (BF16/INT8)
  robot/        - ROS2 컨트롤러, 카메라, 키보드
  configs/serbot2.yaml
  start.sh
  DEPLOY.md
  HANDOFF_20260601.md (이 파일)
```

---

## 7. lerobot 실험 결과 반영 (GX10 완료 후)

```
lerobot/pi0_old backbone 학습 완료 후:
  GX10에서 CL eval 실행
  base PaliGemma (73.6%) vs lerobot backbone (?) 비교
  
결과 좋으면:
  → checkpoints/best 업데이트
  → Serbot2로 새 체크포인트 전송
  → 발표 ablation에 "robot-pretrained backbone" 효과 추가
```

---

*문의: monapi-train 브랜치 또는 GX10 서버 참조*
