# Phase 3 Step D 파일럿 — 조건 2/3: bbox_only (VLM cond 안 씀, gray basket bbox만)
# python training/train.py --config configs/pilot_bbox_only.yaml

model:
  action_dim: 3
  horizon: 10
  hidden_dim: 512
  n_flow_steps: 50

  use_paligemma: true
  load_pretrained_paligemma: true
  paligemma_id: "google/paligemma-3b-pt-224"
  vision_model_id: "google/siglip-so400m-patch14-384"
  lang_model_id: "google/gemma-2b"
  use_gemma_expert: false
  load_lerobot: false

  use_bbox_cond: false
  bbox_only: true

data:
  train_path: /home/minum/26CS/MoNa-pi/mobile_vla_dataset_merged/
  val_split: 0.1
  window_size: 8
  image_size: 224
  preprocess: true
  smooth: true
  normalize: false
  inject_prefix: true
  use_delta: false
  bbox_cache_path: /home/minum/26CS/MoNa-pi/logs/bbox_cache.json

  augment: true
  use_color_jitter: true
  use_random_crop: true
  use_counterfactual: true
  cf_stop_prob: 0.10
  cf_steer_prob: 0.10

train:
  batch_size: 4
  lr: 1.0e-4
  epochs: 15
  warmup_steps: 250
  grad_clip: 1.0
  checkpoint_dir: checkpoints/pilot_bbox_only
  keep_top_k: 1
  seed: 42

pretrain:
  ckpt_path: "checkpoints/lerobot_pi0_backbone"

logging:
  use_wandb: false
  project: mona-pi
  entity: shosae
  log_every_n_steps: 20
